Contributing to Cadence

- run tests: `pytest`
- lint: `ruff check .`
- format: `ruff format .`
- type check: `mypy app/`
- start dev env: `docker compose up`
- migrations: not set up yet, run `Base.metadata.create_all(engine)` manually for now
